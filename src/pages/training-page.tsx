import { PageShell } from '@/components/layout/page-shell'

export default function TrainingPage() {
  return <PageShell title="Training" description="Training status and trigger controls are owner-only." />
}
