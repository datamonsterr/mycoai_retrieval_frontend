import { PageShell } from '@/components/layout/page-shell'

export default function SettingsPage() {
  return (
    <PageShell
      title="Settings"
      description="Theme, profile, and local preferences live here."
    />
  )
}
